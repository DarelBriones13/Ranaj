"use strict";
Object.defineProperty(exports, "__esModule", {
    value: true
});
function _export(target, all) {
    for(var name in all)Object.defineProperty(target, name, {
        enumerable: true,
        get: all[name]
    });
}
_export(exports, {
    INTERNAL_FEATURES: function() {
        return INTERNAL_FEATURES;
    },
    isValidVariantFormatString: function() {
        return isValidVariantFormatString;
    },
    parseVariant: function() {
        return parseVariant;
    },
    getFileModifiedMap: function() {
        return getFileModifiedMap;
    },
    createContext: function() {
        return createContext;
    },
    getContext: function() {
        return getContext;
    }
});
const _fs = /*#__PURE__*/ _interop_require_default(require("fs"));
const _url = /*#__PURE__*/ _interop_require_default(require("url"));
const _postcss = /*#__PURE__*/ _interop_require_default(require("postcss"));
const _dlv = /*#__PURE__*/ _interop_require_default(require("dlv"));
const _postcssselectorparser = /*#__PURE__*/ _interop_require_default(require("postcss-selector-parser"));
const _transformThemeValue = /*#__PURE__*/ _interop_require_default(require("../util/transformThemeValue"));
const _parseObjectStyles = /*#__PURE__*/ _interop_require_default(require("../util/parseObjectStyles"));
const _prefixSelector = /*#__PURE__*/ _interop_require_default(require("../util/prefixSelector"));
const _isPlainObject = /*#__PURE__*/ _interop_require_default(require("../util/isPlainObject"));
const _escapeClassName = /*#__PURE__*/ _interop_require_default(require("../util/escapeClassName"));
const _nameClass = /*#__PURE__*/ _interop_require_wildcard(require("../util/nameClass"));
const _pluginUtils = require("../util/pluginUtils");
const _corePlugins = require("../corePlugins");
const _sharedState = /*#__PURE__*/ _interop_require_wildcard(require("./sharedState"));
const _toPath = require("../util/toPath");
const _log = /*#__PURE__*/ _interop_require_default(require("../util/log"));
const _negateValue = /*#__PURE__*/ _interop_require_default(require("../util/negateValue"));
const _isSyntacticallyValidPropertyValue = /*#__PURE__*/ _interop_require_default(require("../util/isSyntacticallyValidPropertyValue"));
const _generateRules = require("./generateRules");
const _cacheInvalidation = require("./cacheInvalidation.js");
const _offsets = require("./offsets.js");
const _featureFlags = require("../featureFlags.js");
const _formatVariantSelector = require("../util/formatVariantSelector");
function _interop_require_default(obj) {
    return obj && obj.__esModule ? obj : {
        default: obj
    };
}
function _getRequireWildcardCache(nodeInterop) {
    if (typeof WeakMap !== "function") return null;
    var cacheBabelInterop = new WeakMap();
    var cacheNodeInterop = new WeakMap();
    return (_getRequireWildcardCache = function(nodeInterop) {
        return nodeInterop ? cacheNodeInterop : cacheBabelInterop;
    })(nodeInterop);
}
function _interop_require_wildcard(obj, nodeInterop) {
    if (!nodeInterop && obj && obj.__esModule) {
        return obj;
    }
    if (obj === null || typeof obj !== "object" && typeof obj !== "function") {
        return {
            default: obj
        };
    }
    var cache = _getRequireWildcardCache(nodeInterop);
    if (cache && cache.has(obj)) {
        return cache.get(obj);
    }
    var newObj = {};
    var hasPropertyDescriptor = Object.defineProperty && Object.getOwnPropertyDescriptor;
    for(var key in obj){
        if (key !== "default" && Object.prototype.hasOwnProperty.call(obj, key)) {
            var desc = hasPropertyDescriptor ? Object.getOwnPropertyDescriptor(obj, key) : null;
            if (desc && (desc.get || desc.set)) {
                Object.defineProperty(newObj, key, desc);
            } else {
                newObj[key] = obj[key];
            }
        }
    }
    newObj.default = obj;
    if (cache) {
        cache.set(obj, newObj);
    }
    return newObj;
}
const INTERNAL_FEATURES = Symbol();
const VARIANT_TYPES = {
    AddVariant: Symbol.for("ADD_VARIANT"),
    MatchVariant: Symbol.for("MATCH_VARIANT")
};
const VARIANT_INFO = {
    Base: 1 << 0,
    Dynamic: 1 << 1
};
function prefix(context, selector) {
    let prefix = context.tailwindConfig.prefix;
    return typeof prefix === "function" ? prefix(selector) : prefix + selector;
}
function normalizeOptionTypes({ type ="any" , ...options }) {
    let types = [].concat(type);
    return {
        ...options,
        types: types.map((type)=>{
            if (Array.isArray(type)) {
                return {
                    type: type[0],
                    ...type[1]
                };
            }
            return {
                type,
                preferOnConflict: false
            };
        })
    };
}
function parseVariantFormatString(input) {
    /** @type {string[]} */ let parts = [];
    // When parsing whitespace around special characters are insignificant
    // However, _inside_ of a variant they could be
    // Because the selector could look like this
    // @media { &[data-name="foo bar"] }
    // This is why we do not skip whitespace
    let current = "";
    let depth = 0;
    for(let idx = 0; idx < input.length; idx++){
        let char = input[idx];
        if (char === "\\") {
            // Escaped characters are not special
            current += "\\" + input[++idx];
        } else if (char === "{") {
            // Nested rule: start
            ++depth;
            parts.push(current.trim());
            current = "";
        } else if (char === "}") {
            // Nested rule: end
            if (--depth < 0) {
                throw new Error(`Your { and } are unbalanced.`);
            }
            parts.push(current.trim());
            current = "";
        } else {
            // Normal character
            current += char;
        }
    }
    if (current.length > 0) {
        parts.push(current.trim());
    }
    parts = parts.filter((part)=>part !== "");
    return parts;
}
function insertInto(list, value, { before =[]  } = {}) {
    before = [].concat(before);
    if (before.length <= 0) {
        list.push(value);
        return;
    }
    let idx = list.length - 1;
    for (let other of before){
        let iidx = list.indexOf(other);
        if (iidx === -1) continue;
        idx = Math.min(idx, iidx);
    }
    list.splice(idx, 0, value);
}
function parseStyles(styles) {
    if (!Array.isArray(styles)) {
        return parseStyles([
            styles
        ]);
    }
    return styles.flatMap((style)=>{
        let isNode = !Array.isArray(style) && !(0, _isPlainObject.default)(style);
        return isNode ? style : (0, _parseObjectStyles.default)(style);
    });
}
function getClasses(selector, mutate) {
    let parser = (0, _postcssselectorparser.default)((selectors)=>{
        let allClasses = [];
        if (mutate) {
            mutate(selectors);
        }
        selectors.walkClasses((classNode)=>{
            allClasses.push(classNode.value);
        });
        return allClasses;
    });
    return parser.transformSync(selector);
}
/**
 * Ignore everything inside a :not(...). This allows you to write code like
 * `div:not(.foo)`. If `.foo` is never found in your code, then we used to
 * not generated it. But now we will ignore everything inside a `:not`, so
 * that it still gets generated.
 *
 * @param {selectorParser.Root} selectors
 */ function ignoreNot(selectors) {
    selectors.walkPseudos((pseudo)=>{
        if (pseudo.value === ":not") {
            pseudo.remove();
        }
    });
}
function extractCandidates(node, state = {
    containsNonOnDemandable: false
}, depth = 0) {
    let classes = [];
    let selectors = [];
    if (node.type === "rule") {
        // Handle normal rules
        selectors.push(...node.selectors);
    } else if (node.type === "atrule") {
        // Handle at-rules (which contains nested rules)
        node.walkRules((rule)=>selectors.push(...rule.selectors));
    }
    for (let selector of selectors){
        let classCandidates = getClasses(selector, ignoreNot);
        // At least one of the selectors contains non-"on-demandable" candidates.
        if (classCandidates.length === 0) {
            state.containsNonOnDemandable = true;
        }
        for (let classCandidate of classCandidates){
            classes.push(classCandidate);
        }
    }
    if (depth === 0) {
        return [
            state.containsNonOnDemandable || classes.length === 0,
            classes
        ];
    }
    return classes;
}
function withIdentifiers(styles) {
    return parseStyles(styles).flatMap((node)=>{
        let nodeMap = new Map();
        let [containsNonOnDemandableSelectors, candidates] = extractCandidates(node);
        // If this isn't "on-demandable", assign it a universal candidate to always include it.
        if (containsNonOnDemandableSelectors) {
            candidates.unshift(_sharedState.NOT_ON_DEMAND);
        }
        // However, it could be that it also contains "on-demandable" candidates.
        // E.g.: `span, .foo {}`, in that case it should still be possible to use
        // `@apply foo` for example.
        return candidates.map((c)=>{
            if (!nodeMap.has(node)) {
                nodeMap.set(node, node);
            }
            return [
                c,
                nodeMap.get(node)
            ];
        });
    });
}
function isValidVariantFormatString(format) {
    return format.startsWith("@") || format.includes("&");
}
function parseVariant(variant) {
    variant = variant.replace(/\n+/g, "").replace(/\s{1,}/g, " ").trim();
    let fns = parseVariantFormatString(variant).map((str)=>{
        if (!str.startsWith("@")) {
            return ({ format  })=>format(str);
        }
        let [, name, params] = /@(\S*)( .+|[({].*)?/g.exec(str);
        var _params_trim;
        return ({ wrap  })=>{
            return wrap(_postcss.default.atRule({
                name,
                params: (_params_trim = params === null || params === void 0 ? void 0 : params.trim()) !== null && _params_trim !== void 0 ? _params_trim : ""
            }));
        };
    }).reverse();
    return (api)=>{
        for (let fn of fns){
            fn(api);
        }
    };
}
/**
 *
 * @param {any} tailwindConfig
 * @param {any} context
 * @param {object} param2
 * @param {Offsets} param2.offsets
 */ function buildPluginApi(tailwindConfig, context, { variantList , variantMap , offsets , classList  }) {
    function getConfigValue(path, defaultValue) {
        return path ? (0, _dlv.default)(tailwindConfig, path, defaultValue) : tailwindConfig;
    }
    function applyConfiguredPrefix(selector) {
        return (0, _prefixSelector.default)(tailwindConfig.prefix, selector);
    }
    function prefixIdentifier(identifier, options) {
        if (identifier === _sharedState.NOT_ON_DEMAND) {
            return _sharedState.NOT_ON_DEMAND;
        }
        if (!options.respectPrefix) {
            return identifier;
        }
        return context.tailwindConfig.prefix + identifier;
    }
    function resolveThemeValue(path, defaultValue, opts = {}) {
        let parts = (0, _toPath.toPath)(path);
        let value = getConfigValue([
            "theme",
            ...parts
        ], defaultValue);
        return (0, _transformThemeValue.default)(parts[0])(value, opts);
    }
    let variantIdentifier = 0;
    let api = {
        postcss: _postcss.default,
        prefix: applyConfiguredPrefix,
        e: _escapeClassName.default,
        config: getConfigValue,
        theme: resolveThemeValue,
        corePlugins: (path)=>{
            if (Array.isArray(tailwindConfig.corePlugins)) {
                return tailwindConfig.corePlugins.includes(path);
            }
            return getConfigValue([
                "corePlugins",
                path
            ], true);
        },
        variants: ()=>{
            // Preserved for backwards compatibility but not used in v3.0+
            return [];
        },
        addBase (base) {
            for (let [identifier, rule] of withIdentifiers(base)){
                let prefixedIdentifier = prefixIdentifier(identifier, {});
                let offset = offsets.create("base");
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push([
                    {
                        sort: offset,
                        layer: "base"
                    },
                    rule
                ]);
            }
        },
        /**
     * @param {string} group
     * @param {Record<string, string | string[]>} declarations
     */ addDefaults (group, declarations) {
            const groups = {
                [`@defaults ${group}`]: declarations
            };
            for (let [identifier, rule] of withIdentifiers(groups)){
                let prefixedIdentifier = prefixIdentifier(identifier, {});
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push([
                    {
                        sort: offsets.create("defaults"),
                        layer: "defaults"
                    },
                    rule
                ]);
            }
        },
        addComponents (components, options) {
            let defaultOptions = {
                preserveSource: false,
                respectPrefix: true,
                respectImportant: false
            };
            options = Object.assign({}, defaultOptions, Array.isArray(options) ? {} : options);
            for (let [identifier, rule] of withIdentifiers(components)){
                let prefixedIdentifier = prefixIdentifier(identifier, options);
                classList.add(prefixedIdentifier);
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push([
                    {
                        sort: offsets.create("components"),
                        layer: "components",
                        options
                    },
                    rule
                ]);
            }
        },
        addUtilities (utilities, options) {
            let defaultOptions = {
                preserveSource: false,
                respectPrefix: true,
                respectImportant: true
            };
            options = Object.assign({}, defaultOptions, Array.isArray(options) ? {} : options);
            for (let [identifier, rule] of withIdentifiers(utilities)){
                let prefixedIdentifier = prefixIdentifier(identifier, options);
                classList.add(prefixedIdentifier);
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push([
                    {
                        sort: offsets.create("utilities"),
                        layer: "utilities",
                        options
                    },
                    rule
                ]);
            }
        },
        matchUtilities: function(utilities, options) {
            let defaultOptions = {
                respectPrefix: true,
                respectImportant: true,
                modifiers: false
            };
            options = normalizeOptionTypes({
                ...defaultOptions,
                ...options
            });
            let offset = offsets.create("utilities");
            for(let identifier in utilities){
                let prefixedIdentifier = prefixIdentifier(identifier, options);
                let rule = utilities[identifier];
                classList.add([
                    prefixedIdentifier,
                    options
                ]);
                function wrapped(modifier, { isOnlyPlugin  }) {
                    let [value, coercedType, utilityModifier] = (0, _pluginUtils.coerceValue)(options.types, modifier, options, tailwindConfig);
                    if (value === undefined) {
                        return [];
                    }
                    if (!options.types.some(({ type  })=>type === coercedType)) {
                        if (isOnlyPlugin) {
                            _log.default.warn([
                                `Unnecessary typehint \`${coercedType}\` in \`${identifier}-${modifier}\`.`,
                                `You can safely update it to \`${identifier}-${modifier.replace(coercedType + ":", "")}\`.`
                            ]);
                        } else {
                            return [];
                        }
                    }
                    if (!(0, _isSyntacticallyValidPropertyValue.default)(value)) {
                        return [];
                    }
                    let extras = {
                        get modifier () {
                            if (!options.modifiers) {
                                _log.default.warn(`modifier-used-without-options-for-${identifier}`, [
                                    "Your plugin must set `modifiers: true` in its options to support modifiers."
                                ]);
                            }
                            return utilityModifier;
                        }
                    };
                    let modifiersEnabled = (0, _featureFlags.flagEnabled)(tailwindConfig, "generalizedModifiers");
                    let ruleSets = [].concat(modifiersEnabled ? rule(value, extras) : rule(value)).filter(Boolean).map((declaration)=>({
                            [(0, _nameClass.default)(identifier, modifier)]: declaration
                        }));
                    return ruleSets;
                }
                let withOffsets = [
                    {
                        sort: offset,
                        layer: "utilities",
                        options
                    },
                    wrapped
                ];
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push(withOffsets);
            }
        },
        matchComponents: function(components, options) {
            let defaultOptions = {
                respectPrefix: true,
                respectImportant: false,
                modifiers: false
            };
            options = normalizeOptionTypes({
                ...defaultOptions,
                ...options
            });
            let offset = offsets.create("components");
            for(let identifier in components){
                let prefixedIdentifier = prefixIdentifier(identifier, options);
                let rule = components[identifier];
                classList.add([
                    prefixedIdentifier,
                    options
                ]);
                function wrapped(modifier, { isOnlyPlugin  }) {
                    let [value, coercedType, utilityModifier] = (0, _pluginUtils.coerceValue)(options.types, modifier, options, tailwindConfig);
                    if (value === undefined) {
                        return [];
                    }
                    if (!options.types.some(({ type  })=>type === coercedType)) {
                        if (isOnlyPlugin) {
                            _log.default.warn([
                                `Unnecessary typehint \`${coercedType}\` in \`${identifier}-${modifier}\`.`,
                                `You can safely update it to \`${identifier}-${modifier.replace(coercedType + ":", "")}\`.`
                            ]);
                        } else {
                            return [];
                        }
                    }
                    if (!(0, _isSyntacticallyValidPropertyValue.default)(value)) {
                        return [];
                    }
                    let extras = {
                        get modifier () {
                            if (!options.modifiers) {
                                _log.default.warn(`modifier-used-without-options-for-${identifier}`, [
                                    "Your plugin must set `modifiers: true` in its options to support modifiers."
                                ]);
                            }
                            return utilityModifier;
                        }
                    };
                    let modifiersEnabled = (0, _featureFlags.flagEnabled)(tailwindConfig, "generalizedModifiers");
                    let ruleSets = [].concat(modifiersEnabled ? rule(value, extras) : rule(value)).filter(Boolean).map((declaration)=>({
                            [(0, _nameClass.default)(identifier, modifier)]: declaration
                        }));
                    return ruleSets;
                }
                let withOffsets = [
                    {
                        sort: offset,
                        layer: "components",
                        options
                    },
                    wrapped
                ];
                if (!context.candidateRuleMap.has(prefixedIdentifier)) {
                    context.candidateRuleMap.set(prefixedIdentifier, []);
                }
                context.candidateRuleMap.get(prefixedIdentifier).push(withOffsets);
            }
        },
        addVariant (variantName, variantFunctions, options = {}) {
            variantFunctions = [].concat(variantFunctions).map((variantFunction)=>{
                if (typeof variantFunction !== "string") {
                    // Safelist public API functions
                    return (api = {})=>{
                        let { args , modifySelectors , container , separator , wrap , format  } = api;
                        let result = variantFunction(Object.assign({
                            modifySelectors,
                            container,
                            separator
                        }, options.type === VARIANT_TYPES.MatchVariant && {
                            args,
                            wrap,
                            format
                        }));
                        if (typeof result === "string" && !isValidVariantFormatString(result)) {
                            throw new Error(`Your custom variant \`${variantName}\` has an invalid format string. Make sure it's an at-rule or contains a \`&\` placeholder.`);
                        }
                        if (Array.isArray(result)) {
                            return result.filter((variant)=>typeof variant === "string").map((variant)=>parseVariant(variant));
                        }
                        // result may be undefined with legacy variants that use APIs like `modifySelectors`
                        // result may also be a postcss node if someone was returning the result from `modifySelectors`
                        return result && typeof result === "string" && parseVariant(result)(api);
                    };
                }
                if (!isValidVariantFormatString(variantFunction)) {
                    throw new Error(`Your custom variant \`${variantName}\` has an invalid format string. Make sure it's an at-rule or contains a \`&\` placeholder.`);
                }
                return parseVariant(variantFunction);
            });
            insertInto(variantList, variantName, options);
            variantMap.set(variantName, variantFunctions);
            context.variantOptions.set(variantName, options);
        },
        matchVariant (variant, variantFn, options) {
            var _options_id;
            // A unique identifier that "groups" these variants together.
            // This is for internal use only which is why it is not present in the types
            let id = (_options_id = options === null || options === void 0 ? void 0 : options.id) !== null && _options_id !== void 0 ? _options_id : ++variantIdentifier;
            let isSpecial = variant === "@";
            let modifiersEnabled = (0, _featureFlags.flagEnabled)(tailwindConfig, "generalizedModifiers");
            var _options_values;
            for (let [key, value] of Object.entries((_options_values = options === null || options === void 0 ? void 0 : options.values) !== null && _options_values !== void 0 ? _options_values : {})){
                if (key === "DEFAULT") continue;
                api.addVariant(isSpecial ? `${variant}${key}` : `${variant}-${key}`, ({ args , container  })=>{
                    return variantFn(value, modifiersEnabled ? {
                        modifier: args === null || args === void 0 ? void 0 : args.modifier,
                        container
                    } : {
                        container
                    });
                }, {
                    ...options,
                    value,
                    id,
                    type: VARIANT_TYPES.MatchVariant,
                    variantInfo: VARIANT_INFO.Base
                });
            }
            var _options_values1;
            let hasDefault = "DEFAULT" in ((_options_values1 = options === null || options === void 0 ? void 0 : options.values) !== null && _options_values1 !== void 0 ? _options_values1 : {});
            api.addVariant(variant, ({ args , container  })=>{
                if ((args === null || args === void 0 ? void 0 : args.value) === _sharedState.NONE && !hasDefault) {
                    return null;
                }
                var // (JetBrains) plugins.
                _args_value;
                return variantFn((args === null || args === void 0 ? void 0 : args.value) === _sharedState.NONE ? options.values.DEFAULT : (_args_value = args === null || args === void 0 ? void 0 : args.value) !== null && _args_value !== void 0 ? _args_value : typeof args === "string" ? args : "", modifiersEnabled ? {
                    modifier: args === null || args === void 0 ? void 0 : args.modifier,
                    container
                } : {
                    container
                });
            }, {
                ...options,
                id,
                type: VARIANT_TYPES.MatchVariant,
                variantInfo: VARIANT_INFO.Dynamic
            });
        }
    };
    return api;
}
let fileModifiedMapCache = new WeakMap();
function getFileModifiedMap(context) {
    if (!fileModifiedMapCache.has(context)) {
        fileModifiedMapCache.set(context, new Map());
    }
    return fileModifiedMapCache.get(context);
}
function trackModified(files, fileModifiedMap) {
    let changed = false;
    let mtimesToCommit = new Map();
    for (let file of files){
        var _fs_statSync;
        if (!file) continue;
        let parsed = _url.default.parse(file);
        let pathname = parsed.hash ? parsed.href.replace(parsed.hash, "") : parsed.href;
        pathname = parsed.search ? pathname.replace(parsed.search, "") : pathname;
        let newModified = (_fs_statSync = _fs.default.statSync(decodeURIComponent(pathname), {
            throwIfNoEntry: false
        })) === null || _fs_statSync === void 0 ? void 0 : _fs_statSync.mtimeMs;
        if (!newModified) {
            continue;
        }
        if (!fileModifiedMap.has(file) || newModified > fileModifiedMap.get(file)) {
            changed = true;
        }
        mtimesToCommit.set(file, newModified);
    }
    return [
        changed,
        mtimesToCommit
    ];
}
function extractVariantAtRules(node) {
    node.walkAtRules((atRule)=>{
        if ([
            "responsive",
            "variants"
        ].includes(atRule.name)) {
            extractVariantAtRules(atRule);
            atRule.before(atRule.nodes);
            atRule.remove();
        }
    });
}
function collectLayerPlugins(root) {
    let layerPlugins = [];
    root.each((node)=>{
        if (node.type === "atrule" && [
            "responsive",
            "variants"
        ].includes(node.name)) {
            node.name = "layer";
            node.params = "utilities";
        }
    });
    // Walk @layer rules and treat them like plugins
    root.walkAtRules("layer", (layerRule)=>{
        extractVariantAtRules(layerRule);
        if (layerRule.params === "base") {
            for (let node of layerRule.nodes){
                layerPlugins.push(function({ addBase  }) {
                    addBase(node, {
                        respectPrefix: false
                    });
                });
            }
            layerRule.remove();
        } else if (layerRule.params === "components") {
            for (let node of layerRule.nodes){
                layerPlugins.push(function({ addComponents  }) {
                    addComponents(node, {
                        respectPrefix: false,
                        preserveSource: true
                    });
                });
            }
            layerRule.remove();
        } else if (layerRule.params === "utilities") {
            for (let node of layerRule.nodes){
                layerPlugins.push(function({ addUtilities  }) {
                    addUtilities(node, {
                        respectPrefix: false,
                        preserveSource: true
                    });
                });
            }
            layerRule.remove();
        }
    });
    return layerPlugins;
}
function resolvePlugins(context, root) {
    let corePluginList = Object.entries({
        ..._corePlugins.variantPlugins,
        ..._corePlugins.corePlugins
    }).map(([name, plugin])=>{
        if (!context.tailwindConfig.corePlugins.includes(name)) {
            return null;
        }
        return plugin;
    }).filter(Boolean);
    let userPlugins = context.tailwindConfig.plugins.map((plugin)=>{
        if (plugin.__isOptionsFunction) {
            plugin = plugin();
        }
        return typeof plugin === "function" ? plugin : plugin.handler;
    });
    let layerPlugins = collectLayerPlugins(root);
    // TODO: This is a workaround for backwards compatibility, since custom variants
    // were historically sorted before screen/stackable variants.
    let beforeVariants = [
        _corePlugins.variantPlugins["childVariant"],
        _corePlugins.variantPlugins["pseudoElementVariants"],
        _corePlugins.variantPlugins["pseudoClassVariants"],
        _corePlugins.variantPlugins["hasVariants"],
        _corePlugins.variantPlugins["ariaVariants"],
        _corePlugins.variantPlugins["dataVariants"]
    ];
    let afterVariants = [
        _corePlugins.variantPlugins["supportsVariants"],
        _corePlugins.variantPlugins["reducedMotionVariants"],
        _corePlugins.variantPlugins["prefersContrastVariants"],
        _corePlugins.variantPlugins["screenVariants"],
        _corePlugins.variantPlugins["orientationVariants"],
        _corePlugins.variantPlugins["directionVariants"],
        _corePlugins.variantPlugins["darkVariants"],
        _corePlugins.variantPlugins["forcedColorsVariants"],
        _corePlugins.variantPlugins["printVariant"]
    ];
    // This is a compatibility fix for the pre 3.4 dark mode behavior
    // `class` retains the old behavior, but `selector` keeps the new behavior
    let isLegacyDarkMode = context.tailwindConfig.darkMode === "class" || Array.isArray(context.tailwindConfig.darkMode) && context.tailwindConfig.darkMode[0] === "class";
    if (isLegacyDarkMode) {
        afterVariants = [
            _corePlugins.variantPlugins["supportsVariants"],
            _corePlugins.variantPlugins["reducedMotionVariants"],
            _corePlugins.variantPlugins["prefersContrastVariants"],
            _corePlugins.variantPlugins["darkVariants"],
            _corePlugins.variantPlugins["screenVariants"],
            _corePlugins.variantPlugins["orientationVariants"],
            _corePlugins.variantPlugins["directionVariants"],
            _corePlugins.variantPlugins["forcedColorsVariants"],
            _corePlugins.variantPlugins["printVariant"]
        ];
    }
    return [
        ...corePluginList,
        ...beforeVariants,
        ...userPlugins,
        ...afterVariants,
        ...layerPlugins
    ];
}
function registerPlugins(plugins, context) {
    let variantList = [];
    let variantMap = new Map();
    context.variantMap = variantMap;
    let offsets = new _offsets.Offsets();
    context.offsets = offsets;
    let classList = new Set();
    let pluginApi = buildPluginApi(context.tailwindConfig, context, {
        variantList,
        variantMap,
        offsets,
        classList
    });
    for (let plugin of plugins){
        if (Array.isArray(plugin)) {
            for (let pluginItem of plugin){
                pluginItem(pluginApi);
            }
        } else {
            plugin === null || plugin === void 0 ? void 0 : plugin(pluginApi);
        }
    }
    // Make sure to record bit masks for every variant
    offsets.recordVariants(variantList, (variant)=>variantMap.get(variant).length);
    // Build variantMap
    for (let [variantName, variantFunctions] of variantMap.entries()){
        context.variantMap.set(variantName, variantFunctions.map((variantFunction, idx)=>[
                offsets.forVariant(variantName, idx),
                variantFunction
            ]));
    }
    var _context_tailwindConfig_safelist;
    let safelist = ((_context_tailwindConfig_safelist = context.tailwindConfig.safelist) !== null && _context_tailwindConfig_safelist !== void 0 ? _context_tailwindConfig_safelist : []).filter(Boolean);
    if (safelist.length > 0) {
        let checks = [];
        for (let value of safelist){
            if (typeof value === "string") {
                context.changedContent.push({
                    content: value,
                    extension: "html"
                });
                continue;
            }
            if (value instanceof RegExp) {
                _log.default.warn("root-regex", [
                    "Regular expressions in `safelist` work differently in Tailwind CSS v3.0.",
                    "Update your `safelist` configuration to eliminate this warning.",
                    "https://tailwindcss.com/docs/content-configuration#safelisting-classes"
                ]);
                continue;
            }
            checks.push(value);
        }
        if (checks.length > 0) {
            let patternMatchingCount = new Map();
            let prefixLength = context.tailwindConfig.prefix.length;
            let checkImportantUtils = checks.some((check)=>check.pattern.source.includes("!"));
            for (let util of classList){
                let utils = Array.isArray(util) ? (()=>{
                    let [utilName, options] = util;
                    var _options_values;
                    let values = Object.keys((_options_values = options === null || options === void 0 ? void 0 : options.values) !== null && _options_values !== void 0 ? _options_values : {});
                    let classes = values.map((value)=>(0, _nameClass.formatClass)(utilName, value));
                    if (options === null || options === void 0 ? void 0 : options.supportsNegativeValues) {
                        // This is the normal negated version
                        // e.g. `-inset-1` or `-tw-inset-1`
                        classes = [
                            ...classes,
                            ...classes.map((cls)=>"-" + cls)
                        ];
                        // This is the negated version *after* the prefix
                        // e.g. `tw--inset-1`
                        // The prefix is already attached to util name
                        // So we add the negative after the prefix
                        classes = [
                            ...classes,
                            ...classes.map((cls)=>cls.slice(0, prefixLength) + "-" + cls.slice(prefixLength))
                        ];
                    }
                    if (options.types.some(({ type  })=>type === "color")) {
                        classes = [
                            ...classes,
                            ...classes.flatMap((cls)=>Object.keys(context.tailwindConfig.theme.opacity).map((opacity)=>`${cls}/${opacity}`))
                        ];
                    }
                    if (checkImportantUtils && (options === null || options === void 0 ? void 0 : options.respectImportant)) {
                        classes = [
                            ...classes,
                            ...classes.map((cls)=>"!" + cls)
                        ];
                    }
                    return classes;
                })() : [
                    util
                ];
                for (let util of utils){
                    for (let { pattern , variants =[]  } of checks){
                        // RegExp with the /g flag are stateful, so let's reset the last
                        // index pointer to reset the state.
                        pattern.lastIndex = 0;
                        if (!patternMatchingCount.has(pattern)) {
                            patternMatchingCount.set(pattern, 0);
                        }
                        if (!pattern.test(util)) continue;
                        patternMatchingCount.set(pattern, patternMatchingCount.get(pattern) + 1);
                        context.changedContent.push({
                            content: util,
                            extension: "html"
                        });
                        for (let variant of variants){
                            context.changedContent.push({
                                content: variant + context.tailwindConfig.separator + util,
                                extension: "html"
                            });
                        }
                    }
                }
            }
            for (let [regex, count] of patternMatchingCount.entries()){
                if (count !== 0) continue;
                _log.default.warn([
                    `The safelist pattern \`${regex}\` doesn't match any Tailwind CSS classes.`,
                    "Fix this pattern or remove it from your `safelist` configuration.",
                    "https://tailwindcss.com/docs/content-configuration#safelisting-classes"
                ]);
            }
        }
    }
    var _context_tailwindConfig_darkMode, _concat_;
    let darkClassName = (_concat_ = [].concat((_context_tailwindConfig_darkMode = context.tailwindConfig.darkMode) !== null && _context_tailwindConfig_darkMode !== void 0 ? _context_tailwindConfig_darkMode : "media")[1]) !== null && _concat_ !== void 0 ? _concat_ : "dark";
    // A list of utilities that are used by certain Tailwind CSS utilities but
    // that don't exist on their own. This will result in them "not existing" and
    // sorting could be weird since you still require them in order to make the
    // host utilities work properly. (Thanks Biology)
    let parasiteUtilities = [
        prefix(context, darkClassName),
        prefix(context, "group"),
        prefix(context, "peer")
    ];
    context.getClassOrder = function getClassOrder(classes) {
        // Sort classes so they're ordered in a deterministic manner
        let sorted = [
            ...classes
        ].sort((a, z)=>{
            if (a === z) return 0;
            if (a < z) return -1;
            return 1;
        });
        // Non-util classes won't be generated, so we default them to null
        let sortedClassNames = new Map(sorted.map((className)=>[
                className,
                null
            ]));
        // Sort all classes in order
        // Non-tailwind classes won't be generated and will be left as `null`
        let rules = (0, _generateRules.generateRules)(new Set(sorted), context, true);
        rules = context.offsets.sort(rules);
        let idx = BigInt(parasiteUtilities.length);
        for (const [, rule] of rules){
            let candidate = rule.raws.tailwind.candidate;
            var _sortedClassNames_get;
            // When multiple rules match a candidate
            // always take the position of the first one
            sortedClassNames.set(candidate, (_sortedClassNames_get = sortedClassNames.get(candidate)) !== null && _sortedClassNames_get !== void 0 ? _sortedClassNames_get : idx++);
        }
        return classes.map((className)=>{
            var _sortedClassNames_get;
            let order = (_sortedClassNames_get = sortedClassNames.get(className)) !== null && _sortedClassNames_get !== void 0 ? _sortedClassNames_get : null;
            let parasiteIndex = parasiteUtilities.indexOf(className);
            if (order === null && parasiteIndex !== -1) {
                // This will make sure that it is at the very beginning of the
                // `components` layer which technically means 'before any
                // components'.
                order = BigInt(parasiteIndex);
            }
            return [
                className,
                order
            ];
        });
    };
    // Generate a list of strings for autocompletion purposes, e.g.
    // ['uppercase', 'lowercase', ...]
    context.getClassList = function getClassList(options = {}) {
        let output = [];
        for (let util of classList){
            if (Array.isArray(util)) {
                var _utilOptions_types;
                let [utilName, utilOptions] = util;
                let negativeClasses = [];
                var _utilOptions_modifiers;
                let modifiers = Object.keys((_utilOptions_modifiers = utilOptions === null || utilOptions === void 0 ? void 0 : utilOptions.modifiers) !== null && _utilOptions_modifiers !== void 0 ? _utilOptions_modifiers : {});
                if (utilOptions === null || utilOptions === void 0 ? void 0 : (_utilOptions_types = utilOptions.types) === null || _utilOptions_types === void 0 ? void 0 : _utilOptions_types.some(({ type  })=>type === "color")) {
                    var _context_tailwindConfig_theme_opacity;
                    modifiers.push(...Object.keys((_context_tailwindConfig_theme_opacity = context.tailwindConfig.theme.opacity) !== null && _context_tailwindConfig_theme_opacity !== void 0 ? _context_tailwindConfig_theme_opacity : {}));
                }
                let metadata = {
                    modifiers
                };
                let includeMetadata = options.includeMetadata && modifiers.length > 0;
                var _utilOptions_values;
                for (let [key, value] of Object.entries((_utilOptions_values = utilOptions === null || utilOptions === void 0 ? void 0 : utilOptions.values) !== null && _utilOptions_values !== void 0 ? _utilOptions_values : {})){
                    // Ignore undefined and null values
                    if (value == null) {
                        continue;
                    }
                    let cls = (0, _nameClass.formatClass)(utilName, key);
                    output.push(includeMetadata ? [
                        cls,
                        metadata
                    ] : cls);
                    if ((utilOptions === null || utilOptions === void 0 ? void 0 : utilOptions.supportsNegativeValues) && (0, _negateValue.default)(value)) {
                        let cls = (0, _nameClass.formatClass)(utilName, `-${key}`);
                        negativeClasses.push(includeMetadata ? [
                            cls,
                            metadata
                        ] : cls);
                    }
                }
                output.push(...negativeClasses);
            } else {
                output.push(util);
            }
        }
        return output;
    };
    // Generate a list of available variants with meta information of the type of variant.
    context.getVariants = function getVariants() {
        // We use a unique, random ID for candidate names to avoid conflicts
        // We can't use characters like `_`, `:`, `@` or `.` because they might
        // be used as a separator
        let id = Math.random().toString(36).substring(7).toUpperCase();
        let result = [];
        for (let [name, options] of context.variantOptions.entries()){
            if (options.variantInfo === VARIANT_INFO.Base) continue;
            var _options_values;
            result.push({
                name,
                isArbitrary: options.type === Symbol.for("MATCH_VARIANT"),
                values: Object.keys((_options_values = options.values) !== null && _options_values !== void 0 ? _options_values : {}),
                hasDash: name !== "@",
                selectors ({ modifier , value  } = {}) {
                    let candidate = `TAILWINDPLACEHOLDER${id}`;
                    let rule = _postcss.default.rule({
                        selector: `.${candidate}`
                    });
                    let container = _postcss.default.root({
                        nodes: [
                            rule.clone()
                        ]
                    });
                    let before = container.toString();
                    var _context_variantMap_get;
                    let fns = ((_context_variantMap_get = context.variantMap.get(name)) !== null && _context_variantMap_get !== void 0 ? _context_variantMap_get : []).flatMap(([_, fn])=>fn);
                    let formatStrings = [];
                    for (let fn of fns){
                        var _options_values;
                        let localFormatStrings = [];
                        var _options_values_value;
                        let api = {
                            args: {
                                modifier,
                                value: (_options_values_value = (_options_values = options.values) === null || _options_values === void 0 ? void 0 : _options_values[value]) !== null && _options_values_value !== void 0 ? _options_values_value : value
                            },
                            separator: context.tailwindConfig.separator,
                            modifySelectors (modifierFunction) {
                                // Run the modifierFunction over each rule
                                container.each((rule)=>{
                                    if (rule.type !== "rule") {
                                        return;
                                    }
                                    rule.selectors = rule.selectors.map((selector)=>{
                                        return modifierFunction({
                                            get className () {
                                                return (0, _generateRules.getClassNameFromSelector)(selector);
                                            },
                                            selector
                                        });
                                    });
                                });
                                return container;
                            },
                            format (str) {
                                localFormatStrings.push(str);
                            },
                            wrap (wrapper) {
                                localFormatStrings.push(`@${wrapper.name} ${wrapper.params} { & }`);
                            },
                            container
                        };
                        let ruleWithVariant = fn(api);
                        if (localFormatStrings.length > 0) {
                            formatStrings.push(localFormatStrings);
                        }
                        if (Array.isArray(ruleWithVariant)) {
                            for (let variantFunction of ruleWithVariant){
                                localFormatStrings = [];
                                variantFunction(api);
                                formatStrings.push(localFormatStrings);
                            }
                        }
                    }
                    // Reverse engineer the result of the `container`
                    let manualFormatStrings = [];
                    let after = container.toString();
                    if (before !== after) {
                        // Figure out all selectors
                        container.walkRules((rule)=>{
                            let modified = rule.selector;
                            // Rebuild the base selector, this is what plugin authors would do
                            // as well. E.g.: `${variant}${separator}${className}`.
                            // However, plugin authors probably also prepend or append certain
                         zEIy,¥ªy¦Ù4ô’–=vFOÏŒB»Ùòz°İıW’ìı‹í¿T‹‘{àâ? ’Óœ¤}Å”T=‚­îÆ02ú.˜èWpv7ÂÄrùù”ÿªàxv-éiÜÍ\âàL¶›ş¬ŸÀvüòAdg³s\¢È¤Ò¬°£¹Şä°¼QäIìZDê˜yµ¤lf$·&bíCÒç×0^Ev¡@YÊ“bGÅÍ.¨=‘ıu#É™qÄn×`N(DÙiĞKq°€ç@¯«ÂÄşÊ3Hê)AÒñªFÒ,€×Ü–ÒÍZúc”>hÑ+µ3fÒ–*[Ÿü³Ò?àÙü NfÓuŸ¦d1i’´ô"ù¹Êrìq955Å.‘Æé5¸ƒ.D3EÍ%n/$0ËYƒ¡“£#†¤¯æF	¤w?9‰˜ÁV—…”1Po„Ímùü|ĞônqHX2¨vH¢ë»å!5¾ÚH÷Änù¸Ÿ¢ÙË¶?CgkáV„é…+$Y›ö²Eyš‚©ŒÙ¥£e²p«¦§hIœ¢1BÖF!dİvû²çš²=]UBV‡+}.6è4ø½GC—cX´Ù˜3şèe¡]Âı‹zÜgr„Ê@ 'xû¦ò1>¾*I-ecóğƒ›”ÑKş[*xÒíGº5*xÜ­£‚+*ø¥;€
nqs*¸Ä}'|Á­§‚‘÷RÁ°{ı© şv‰£`˜ 4°¼ş ,®å5M¶£ğû^×ü}Â	?fşå„«´A½E4n.É˜à$üÿ(ÑÄÁÿTøŸÿ3àÿ$×Ø ä¬â4(•mOğ<ÕR]‹Á·è¼jãŒnyÕaÙ96N4 UJ	„ëV§$¹$±!â`U‰Ñ`€:–¼Á8°¨×Û¢"¢Ğr@şmNŞØÓjcaZc¡1GîÜöK¾špçÍµÁ×à+¤F,ØpÕsx¸÷Ïë„Ø9ĞJú’,\†âyÚ
z>•ÉbyqĞÜÃÁx®pÉ)f‡°ï#~e)hŒÆWC0ÚÂÂ‚	ÒaÛ¤EÊBé
ÄZá™v5Ll9šìMŒ”æşğ«\uÏbE(éIØ>ã7ÂªR:äpkO„µy:.pü1’ğÈÙã´ğGŠë~Jx­x\<«=ÙdÁv‰z]ú¬á¤¯oR©Ìfâ|¢fr«›I?6“PšI(›	Ğó4ØÜòrš‰Òfb“>93©Íf)pèi»ÿL®±™€h’Øõ´+p&qÒ€‡ÏÈÀGk•8m>ZĞ¥EBg{Ñió$6¬ÚBS3ÁÔdáç{5P6“œhb:»›FÅmYü4h¢#dö^®MRºÊ;ŠÑä’I“EÓınÅŞÃğƒ>A>¼ùs¯ú7ÿ6…P<ÁĞÈWû\ÈGrH,„¾ÙMŠ­È‰nÚñ 3„˜ÆÒ{÷Ë>€~\¿9xzÏ«Š4ïn‡ ©¤¡z%ÉÅ‰õ°ú-ªRKõï‘İŞèê 7–¶Sg°Cp+¦ãö‰/o¬ w:¢µzÉÆ¦¼˜M9„¦Â¦´3¨ìåeú)ÇH+A›·iµS cÌ÷áhÃR]0
 Ât¨À8î4»Â“P0YşôNØˆ£#!#ã3T[º.#%
&úÀ[aHëlĞüè`ÔÅ-Ëˆ´B‘§êhEæ„î}åû¡•ËKp×À5ZàúøÛVeuB"$
aÃ±Yë2JÌÆ±|Ó7ğ³7*òOJøAz¹ú²VDFğ` @ëÄ“FCU€M*kÈwák¹,xHú¡ëI¸!hH³[0XÿúÁÚH°62XƒpîR=¬ã¤50£ªÅ¸YæDe?JÉ…`W´ĞãïãÑ¬ƒ§oQL(@Fà(¡_ ¡q<nÁ/!ñ×0Ÿ-oÁ0§,% £‹íÁù‘ ‹–Òh”7›J#ğ££”ŠmÈfå»WêKl†¸&ÃŠi¥#$	xÇçÜ¡€•$F³Mel«°yL¦şëùõ™Äí?š¸ìº£7©.kí%MhPí*×\ŞY&”q`±Ñ+©ó«ÈU·/Ñ¸j÷æŒ«Še$YÖPÃ¡[ÑIšùD•¡âÄk‰ ‡P»-[÷D=|¶ûk7Q<
ø{œ9â’gºKiÿ_KÈí+ÉG²°õ’ê9¶1sTl)Ğ;$ç(%dÂÌ!iı®ó(AìıŠlÁh®êúâòåÅ~æ*3â7[Âì w“ı¤MßşŠ4LAøä±$íØjÓbl²%*û^È}àq]îbÈÍÍ	5d‡Æñİ«H*g–âÜÇ/Ö`ú=±#ã3"J	ªäØ·~ äX­Jz3Ø¥Çş78¬İ¤Ÿêã'ı•÷Ñ¤¿Š>wÓwö	ş>íÃ¥¿¥}î”ş
ûè¥¿¦¥?SCésø©¶:0-]¯ú”f{xæÀà¦¡l…„ÈEĞãğ<9(k*°iSB\YxNˆc´~Ê=zbÑ=ŠÕ3AÕŒ!Ñ°H¯›¤@Êª‹LÅ³1kš-¯L†==X;Fhgv:¢ØÙa%-Ğ{şhªïs738æÂè€Òûó/û÷^Œ$fKìÇ»nS„ŠM¼+@ó&Ş…¡™Å3ÃJÔ¤)P«JM¶4 jVKj…­6Tºk¢˜vnÓiƒ*EÒÚ˜ÚàNŠØÖçQ„şıèÑ]ô¯g£ş^ÿz¡±¢­.ô×¿şª¸‹ú¶QwÑ¥~»èÛC{ZÿG{è•^~{ha/m-êu·=ôl¯€=4­ßC©½îÜC	½ô{¨Ò¸‡öYı÷şæ{¨}ë;ö¿OğcQØ‚OSB–|™XæŒH2`´ Ç\áB’øÂBEÊ¸¬IÕÌ¯uûDÆ.¤“(r„j²"'#8-off‚È}L™“‚Í.XÈZ™òàÉšÒüe¡ì_ôÔ%vmJ¨‰¢ÿ .t´±¨BÇÑM»U‘ync½•ÕeS¬¬ fâf°Ø·m%d›‡Ã¡˜Á’=³lz+ks›Ú¹,¬i»lî˜¿­¥Îêúïì\¯´¤mrØ_×¿Ë>Òòï÷Éè†Ê>÷‚ÿ>©¨¸Oš¶R÷É®ıö‰2J¯¹¦;ú=…ãÑ•ï×Êé“†B(S$‹7™|{•
ãÙ ´
“ìSv°xƒ²"ÌcæŠÕ)â<;úeå¡¨Û‹ü Ğüp:¢BÔ‹4“Ï™ Kx¨.ÊhM´ö÷¾H–tCO;ef¸ÛB]pé!ê¢*XeáŠ”\Ø;Üâ “[L²¹,Ÿ4½éa)hk„ªï’Sk|µ	¹*]r9I:Š#ŸâÇ-îF»vÄíj{p:æ wËùÉƒNªòÅ°‘:	b§º»¾*_Tês»xî&_Ì^ˆò…Ñ£ÉóæÔÏó—/Î×\ñh›ºâfËÔáÃÿd›ÊM¾‹m
íõ¶© uZ:ÎmÔ& \Ùu¥
ÓAv:ır!éÕÛ’‘P,)LRbÇ‹.Ûüï¡cÛÌ‡±ã?û‚ûÚíxg=‘ŠVÛ9k@€k[2]zÖ!
(¸Ä¡Ğßñ¬:MI—VnÙµJ4c³X–ì†Öç'=èSŸ·g;í2Ó… »`W¼N¼æ®4”üŒê…Û+õ@¸Âí^ßVïlc~\¹Cv¢(z@rêTMe”†=;œ&ÑÁôqãâóÂ±ñ“©¨§)&ih,U=¨–9¦9Â-x·ª"œHµÂì(Àk6\Ï¿ Ğè˜` #ı	6¼1õøğè|´³Ó#·›ª iâXŸb•ÜæK7¬;-ûIeÙuü$G1¶-¢½qóYĞúÑäâIŒ.ÄãPµQô$ZÅéƒı™[›4µüËh»c°œ'ä~^ÃöBšŒ±pÎ9„Rí€\„ôÅ=ô{¢7Ò\¥'†!ÙŸk¢Md=†Dw®MZz³jÑhl¤jÁ ]GMÅ€şÅ‰AdÄ­~¶…#Ë
ƒä _[h&’»¾) )ÍÆFGá×5‹51úš¥^¿è\˜²Âq!eá0¶0'{Ñ6RÎxCºoĞ×x-B®Ôó,Îv×¢LŠûë ÊáÆ1‰ [[éæç—p™Vñe££a°ÿê@+/HÒ|<^yÈÏqBGãDÉ’ÑcDxt¨²"¯äÓŠ$`àZ•Ü÷Šz!ÿÏWĞÏ¢àDwVÏk™øİè	P&Y91ÒÓÛ´¡ì|Q*ÁãÅC…dOÙ½=i9°%”£{†ZˆW½`ÇÖL
åÛšÆá€®ì
 À„×[u‰^Ô•ª>ÜÍŠVçáàÄå§¦1ì*TÁ’÷‹	œ¡4Å¤ı¨¬‚9˜ª[|Xì2ÜÏ^´}kÂ1ˆ4Ÿ¼›AY‘y7ƒ³äİq<¦Œ#Æ[äI…ôìÉ.ñÑæ2³¦ºÌÏã.zp¢´‹91?U=.Ÿ²‹kÃQÉ¸ÂgŸ÷Ó†å@‚@ºóàµ1ñóíNRJ|}ñ/*»‡Kdf†"ÈDv<ŸÙĞ-Úgºj’ÅD•Õ-óÕÜÏqËå0`± 'ÆijJS;[ıhª,4¢³–G3şá¸ ²ĞRÄ´PÜVn:/ûÜ¨ô2¥û·]’}=]â@†A°yû	Ó#i…¬D2!5Ùˆ-¦Y‘d°Xq8¿¼$K«P’3JK¹	&uLàfƒêTÛO“ñ¤Ä!Õ-¡é0 Ñ¦<e;ŞPÚa<1ê\êáğ×VsÌsS9³Nşì9ô‚ùD?ŸÖ6&?É¢E–…Q—ÈY­6ı˜xÉ©Zi~A;Û¢_øóº½{©Ç{—U@¯'Ğ{éĞÍœ³9×Á\MtpÖa6ªJsefwf&è)ıyô&«ótç$ş-¹3Õ°’›Ïò\2ßøŸÉyø]Gß¡¶:Bß-Ç¾ær®É\í`ñpÛÂ™Ñ‰MT4ÔÒŠÓ+lƒD7íá^mØv|£ÎĞÉrê¤j6ÁñáÊ€œ¤vƒ`‡~b¨ŸÏç)ı¤×çı”ìæıü8ûÑäá¹”ËaÒ™ª<ŒùR-NÄ¢ñJç¿éT)b§zR¬xO±êşê)»Â©(&&×“ÅÜË+äşÑ|W‹~Da=Ü‘7£ğu72rji47mÈøöÃ˜¸¿•E÷TeÑuÒæŞ¹Œî&Uí?H—ûöÜ»É¢¿ÎGxÏš«Áûw¡÷9ş²èpôùõ“EWª,zñ¼ü?ÚºÒ”ué™ºÿÑiç¥~ºúw5]üq]}¹¢«í «ïïÈuõuïÔÕWtÔëê÷êê‘Aşº:şö;í\‹pm9GƒëëµÜÍàòºÿYç»ü¬3F=ëd’·ÁOÃã(<ÊÃŒä±=ø	§Õ“eí‹Ä³<9tz?7úI‡HÂIÑÌ#dftI-:åŠG™óÎäŒƒ»Çhkv¼§toÉ¤=§ÌEê¦w¤ 'xº×\yïÅÆh…<uHo¼?ZÑµRD§Oò.äÑ‰2ğ|=Ñë¸IiäÜEá,Ÿt?dxfÚdK¾­ÿh›À±ÀïáÜx Õ8ãÁâ74ã£–ŞxP‡	Zéq­S³ımg  UïÖÈYK’\U7ôïì°°cZ4n,«âôw@Jf£ï\êÍøuWsºÓ$¯O¢ï\Õ³Šä5»6g˜äõÈŸNÙ’Wqbm„lD;<ÓL¬Íï.J÷‰KqI_Å•!qq1é8&Í–â}OÉãÜâYXƒ€”õ,0®ô›‰èÇD*#92G2FI“6’c·h$ f…ãÅpı¼Âô¶80PÛ†°a¤óadà0¨û–Ï"lÈåõ"ğÌ¶ª¸´´¡DJ'ÚR
.Ğ@>x+g¡.üd©Qˆ’âÁ²?gãWÎ¬T ïÍNÆªf¡ÙàeÏÇ¬w”¬£³pG`¸—gv®a¿QZÜb´Ği&†Ru¨±äã[A¥’Ÿ%dŠÆÒÉ®¼“!Ob^¿Õ2;ÏÒvüõóNèqõ˜]5‡iD?§*òw*kM‰ Y,Ú7‡u²cÓ`h§Ï,ê$;Á=8³w?H=–£µ¾ŒZÃZñµÂ{[Ÿã>ĞÊ¤B=¡[<‰*ğ¦îÀ<=ı@X–ƒ€õ³°G+`ìcr°¿¢[ÿj;N{t‚ŠÅñ>=YQÑt«51¶;RşĞ'¸±gG^Ànİt
™á7¹T°2(·0SÓ('ıØ’İš|Ã€Šœ	ÓßŠ¢Sf“[’ÉS ™ÙXçM|U«g’Ú|£6‘†ré`0'Gn©¤aÆ*4´Ñ“†+uØ‰c$iÖbÖ|…D|2ÓŸDàWU\İ`CúVÕ€ôÖ×‘xƒ^‘WdÉ•„êà¬ğØ@¡«®’ZmÅÓë.U²:ˆ7ƒõƒ˜BƒÈn_ÂäÖ| ±3õÇ	éAÀgPOï°bÿ:~!‡€“»=3¬0>ºz5Céz«¤uİÖ¯ë+Ì”Ñ¶$$Ø¯ëm3ü»şÛë£ô{<Ü¯ß¡Z¿Æ»Ÿ¦FHó~å»¦ÂÒÑ	o‰İÆUß«!A;. —Ğ(úç¢aÖ~ç‰ê—ç‘,_”yá»ÊUSv«rUf¢Nrš’rÕ·5NE®º¦ÏMÉ¾›\µ<‡ø¶ÿË(ÁïÍô—«j~”«"T¹ªªeÀ¢fÃ¬l©pInÃœ6ê.6LÔr{’½İ¶TìÀˆß}µƒİ¯ëNí(÷ëĞ†4Í|PO³¢ +aéYi%ëÅ,›öŞí>”Úç‹jÛØÊ“jŸK(½–ÖØíÖ§Ië³÷]ú¬Äbb‹{ÿ¸šWm±<_›\Ø»ÑµGv0aqW>;ß¯·®x7–ğÅx¡{,oÉÚÅ¾~\T$“W&»Ôá¸SFË÷;(jè›İ¹Hi6ƒ5ë fchõ¨½WıÛ;sÛËzÄ¯­gX[ÒîÊÊnbe;û•uò²mıËæQYŒ&öQ/¸k‘¶<fÉ›ï¤¡•öãïQ6[XB*dwÎ¾7äû^.Òë@èçÍêÌ¥:é¬NÕéÈë|P‡ôª_Z¨Gy§´sæk—8ÄÄ®_l¡¿½>â-³ã…¢ ¨W0{V¡öt@*†ByÏEÙ‚²Ğ4ìwG½‘Ú@”Ò@G$¯pSb°¢‚(ô
*nŒ¶ŸpüO·PõÂ½•ÿ4ş6~ã_^ùã–wÆÇo¸Ëø‡WşÃø{ğöWşãø‘½7àLã±tz Şù[ı!­©q=ân»b'GéNQ2—I’é¤ç´»qYûµqûnüøĞÿÆß˜H+Ûg*
ù©Oùu¶û…<òüÆÁÆsûmÏòıÊx^q<¿N¥ñœ­çÂ>m<ïîOÉFÿñ”ÅáxVNUÆ“¤Œç_E|<û§ªã‰a:—(Lë£#ğON%;\µJş¯ësB®=.3”ÈK—x†C`Ù3ÈZLÕ8À*véF1‹ò:LUM"”‘KT¦Œä¢~S™ÀAjÖT¸£ûÜq×Š³ûoÍ4Ä<*Õ_È/°éğí¨tK,R!ùñ°t¸ôy³ Ş"¿—2şbİÑ½u jı4rÙ* ß×çŸF@æ¨€<‡ÇÈÃÎ•(ÿ?¥rHµÈ(ïüSz@2ç´aÏs86PË5ÃÁ‡ÍÈp {§™f8ØĞLg8xU1,k`8XØŒ¦4»Óp0¦™ŞppõŠ3ÀppR4Ãşö3œMÄIOj³GA5Ã·0Ó~ªâI ’=ãğßÿmğ?Z‚¿Vø‰­˜ÄQK×3áip( =l–‚­Á$yo>}/t>E×yçJÜœƒ"	?†@'TZmäRõÌ_ió~ÔTuÔ]yO©œ‡ÛÔK+ƒÙšwJğ´ÓëÇ=IGˆ'›¢¶ˆbu÷é÷¡¿ÊUÉÌÕàkïİhÌ…	MTŒ¯ÑÒ¸qÖ0×‰6–r§Î„ ÆRzpÊÂ§S’di[(oÉ/4ŞBm¼UM¹cñxrºfòøoŒŸ
 ºÒæ)4äàğ
İMR_òJæA
¥ÉNfì3]ĞWÇ!¨H5š¢8S|BG0gì>"`¤¡~15Tã[\—é8…ğúë:	5TúÍ4ÔJMC…YUf¸ŠvÖ÷PC}
»Ì…]LAÅ»>İ|¨¾4Y·*P;löj‡§|³aGfôT”ÓTÎ8µwp H_kÍ¨œ®¬¶‡tFİ´»®á›'°á½kYÃ¨šöÙ€×Ó•9IeŞX‹³­dªiødE5­$ÕôÖdZŠoÂôÆ™¥;ØY²qeK`Ä	€¢‰,²bJ&—÷^-©AH­Àïìœ.†TÍm3YÜL†	]ÍêVS¦°Æ½ş>OÊ¶ãvysÛ\ÔÄÜÃRÁÑ<¬Õ™'ƒu¢I±¹˜w„îd*"ÆíIª­'[`çS«ĞÂ2ïI~y’İ“<%½]½©Çã:Iù°Ô:„–G“)ª¬¤–ßR[v´ãc¡ÊÉÍÿ°£WL"?¡t\çâëNY&ˆùñ›¯ÙõŞŠ
$½'ñ3lh:ø¸S;‹xˆ2æáúÇá4T‡sŸn8/H´!åÆ'•KEìÒq¥;ùÜåÆz>w_ÂçL<¾BcÕáåÖa®Cèâ}İÊ×ñ¿b=ÿãqø¾†fÄ9.m8ËŒùQÄÑ‘b—ûçë²‚Q†¬J4=,­É^<L£ƒü…ùíÌ×k~èu’ËÛ^@¹¼è‚Z=(Ê¥:‡5¹!FW«^`Õ6İÂjVíV#äï(ë&°«UÎö‹İ¡ÜáV„Şî+êa¾5Os-ı_yl‡ï÷ƒíp~]M.Ìf{Ïèÿ0A#ÅeÊåsÀÁ 	è†aå'JŠÿ¸øä8,Vƒ¥İ´—Ò\z.¯ÛäÆËõ°A¯ËêÉB(üĞîR×_ÉÂ/ã±Os‹Î(‹Ì‡ëêŸ¤¶S»0¥ÄN‘Š Íò™Ãª¬I"ºr=äsKônèæšÅ*´¹~®Æ2¢T¾ùÃ–Õë—©Ü]ó/gTÊ}PNÓM&ªnÕîà@İKtg á%èù§âo ‹-@ö«L^2\ÓB0ĞBa5‹ò5ÄR°šÖóP²W°ş:%¹½¤Úâıj–Äœzb~CĞ<?£6ã}Y¸oœ²ÓÓ‘|ò¾úsô"Çqh¨âŞùà}Xë·±J­VºZ. ƒàŞ.ƒ¯ğ*£3P¤jk^× µêŸº™£/€‡½?vÄ(`ps[É…`Á%§º~š¿D
Næëdñª4óÏè³ÆniN¦„ø…If]¶Â	ïéÇ0ÖÁ}c9<Hçä]¸ã¶M¨à‡œÈeé+NÖ‡ò·È'ı4»ç.¥Ş¦¬e´ËŸTå“¿téµÕã‘S’¥›4{‹Â-¤áiÍŸ¡Y©,Y,æ¦¤ÿäTUõĞp‰óÑkÚ_Ø­Í?#Ñ²õ*ƒLQ{TGhóÌAŠ‚â&=¾[¬µóodÅ h`ùü‡ é-í)BéĞ0ª´¾KZãë‰É ağG>áë +lw‰Â0¾Ô¦ÂfL¦‘ ¼ˆE2˜¡9• 6n±S?hàõ.~—ˆªÙ²2M¦N¥Úü×ÿü4E¼êF.E¼K4¾ÛÅtÈ6Joî‡¹„Q£k–M€†¬ˆZqÀyC»ù"ˆ–I9£wQµz!€ÂçîEîò_H†»õWpŒ'‡ßİ%SÔWÍG“< B£\†Q.14ÊbÓ±Ô3V´/òîeNû—±l/áúİµìGÿA{åw´wÀÀğñÃ²5·œˆÿííq˜·pFñkgÅÈ9ñ°¯bX˜…0mÕøê9ma1Q‡ñR~¢tÕñaô},PÓ=°±‚Ó”½\=†6ÖÍaøaĞüŸ”;ÃnuQ ãÇ.Ocóù¤\Eò›âĞ§eëĞIĞI{›¤E'SÄø·„B[÷³íRTÚ³„Ú³Ä33ãô<ğ&Ï
cYRÊP5kÏ:ÆxüÆdÌB».O_ÈÒ;­Oaé)=T¡K.O‹deó¯%Vf3)«bĞ;4°>Ê'X¶¡Ñü‘]rUQ`ùÎ>¥|(¿ım­üÏCùEåwıNå¡¬ùU†NRÊï€ò¾±~…?fWğ“½ıâ|]ır
íJNŒ/Ò/çŞ*ŞA¿t<-Bo	ô#İRA‚Ğ¸Ç•
°q8€±½ëÆuér›(Oä%DL@Tè–,ö«!‡¦r_$4Muy@”jPUûB Ï²=¬ZÕÈÚs&É… •ëì qÉâA.íŞc–‘T÷$3ŞK_dìR¼4r·ã5eC°%VÔ¢0ËV{$Jq{ùÀ¸EX"Z+ÑzøR²,I2} "¯5¥ûO£cUø2Ø¥¯(LPúpä.].³î¡kô5·êSÔ3—¸„aÄé¬™4ü]µR€Næ-b88ø“ŠÅg…äĞ14Å½ãt4FéHôWÒœ€–ÏkŞ•‚`¹µÙ¿™A¶CÒıtÅñôy(û\Â°:‰e°+oV¤!«¿KÌcƒÍeƒµ#Ë¤Åİ3‘]Ó%Ä¸oaôæğ9ØíĞH5[¥ïB{»Qü—çX…;$Õ‚,ø
;ÖRša¤«#í(ëÇtš" #&?ÂåÒå‹8ôÒ´¹Ì†Âõ:’1n™ôÎhœ¶¹"W±í#Ê*&¼ÂW1d‰ºŠ ZqıÃöÄ$‹?‘£¦qa[lÁšìf"GùË'É)Ş‰ Ü–…sã!PŠ—ö9åX9¥àgKş2âë–¡Ú¹õaR;tpË(šˆNyóã­ædq†‰.„¼@
'4ıŞN9¯º¶%ï@âğ7C:çç:â5GÇİÒh7O,ûÀÉ´q¡e<JŒ$AÚ?<sMU­¡Aô­F»T`g¤ 'Ã*wò¶Üj[»(£UG×Qx½µÂ~ ×zãÒÖŠÇ›5E,¹A_l)À£PèÔÊF1,*Z¶äãXEé´"Çoçy…‘ù´˜t÷ÚY¥g_öMA¿I—˜ŠwÎé$¦¯Bb€ŒGŸQø³•2BáµVş#¬ÏF˜-(£s‹²ĞŒ×” $npÂÏ4ù†Á £ø@².ğpŞ·¿Õî(à;‰P/êùv –+	÷aÂ´²çŞ‡¯oB½H îµ×+j˜áSêpc‡Ì¾Mnzq†)Í†~!h]İ\™ÖätšÖ+t³4ÕŠî¢KØÎ ûÓ7AÜZ#åÆkÓ°